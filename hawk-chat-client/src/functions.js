// this function simply takes a sender string, a receiver string, and a message string, then 
export default function formatMessage(sender, receiver, msg) {
    return {
        sender,
        receiver,
        msg
    }
}